{
    "name": "coda-packs",
    "version": "1.0.0",
    "description": "Monorepo for Coda Packs",
    "private": true,
    "main": "index.js",
    "dependencies": {
      "@codahq/packs-sdk": "^1.7.5",
      "dotenv": "^10.0.0"
    },
    "devDependencies": {
      "typescript": "^4.5.4",
      "ts-node": "^10.4.0",
      "rimraf": "^3.0.2"
    },
    "scripts": {
      "build": "tsc --build",
      "clean": "rimraf dist",
      "build:all": "npm run clean && npm run build",
      "test": "echo \"No tests specified\" && exit 0"
    },
    "license": "ISC",
    "workspaces": ["packs/*"]
  }